## C++ modules through CMake (macos)

```
  import std is not probed: the SDK's libc++ has no libc++.modules.json
probe_named: pass
probe_import_std: not configured (no std module to import)
probe_mixed: not configured (no std module to import)

std module sources in Xcode and the SDK:
```
